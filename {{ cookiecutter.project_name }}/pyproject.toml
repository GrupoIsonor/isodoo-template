[project]
name = "{{ cookiecutter.project_name }}"
version = "1.0.0"
description = "isOdoo project for {{ cookiecutter.project_name }} by {{ cookiecutter.project_owner }}"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.cruft]
skip = ["compose/.secrets/"]
